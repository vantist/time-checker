## Why

Currently, the classification details in the `serve` (web dashboard) and `report` (CLI command) are combined or missing from several aggregation tables (By Model & Role, By Project, By Agent, By Work Item, and Sessions log). Users need detailed visibility into all four token categories (Input, Output, Cache Read, Cache Create) across all reporting metrics to understand actual AI consumption patterns and optimize pricing model inputs.

## What Changes

- Extend reporting structs in `internal/report/report.go` to support cache-specific fields (`InputTokens`, `OutputTokens`, `CacheReadTokens`, and `CacheCreationTokens`).
- Update the SQL aggregation queries in `internal/report/report.go` to group and aggregate cache read and cache creation tokens in addition to input/output tokens.
- Display detailed four-category token fields in all 5 CLI aggregation tables generated by `FormatText` by default.
- Add an output flag `--output` / `-o` to the `report` Cobra command to write report output directly to a file when specified.
- Introduce hover tooltips to show the 4-category token breakdown in the web dashboard, avoiding horizontal layout overflow.

## Capabilities

### New Capabilities

(none)

### Modified Capabilities

- `report-query`: Extend the aggregation logic to group and return all 4 token categories (Input, Output, Cache Read, Cache Creation) for all five query dimensions.
- `report-text-tokens`: Update the CLI report table layout to include columns for all 4 token categories by default, and support writing the text output directly to a file with the `-o` / `--output` flag.
- `web-dashboard`: Update the web dashboard tables to show the main token counts with interactive hover tooltips that break down all 4 token categories.

## Impact

- Affected specs:
  - `openspec/specs/report-query/spec.md`
  - `openspec/specs/report-text-tokens/spec.md`
  - `openspec/specs/web-dashboard/spec.md`
- Affected code:
  - New:
    (none)
  - Modified:
    - `internal/report/report.go`
    - `internal/report/html.go`
    - `cmd/tt/report_cmd.go`
    - `internal/report/report_test.go`
    - `internal/report/html_test.go`
  - Removed:
    (none)

## Source

Derived from brainstorm plan: `.spex/plans/2026-06-21-brainstorm-detailed-categories.md`

## Implementation Approach

Testing strategy: TDD — write failing tests before each implementation unit.
